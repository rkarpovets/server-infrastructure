# {{ ansible_managed }}
apiVersion: 1
datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    jsonData:
      # Lets you jump metrics → logs by shared labels in Grafana Explore
      maxLines: 1000
