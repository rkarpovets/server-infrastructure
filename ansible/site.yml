---
- name: Base server configuration
  hosts: "{{ target_hosts | default('production') }}"
  become: true
  roles:
    - common
    - docker
    - security
    - role: k3s
      tags: [k3s]
    - role: monitoring
      tags: [monitoring]
    - role: logging
      tags: [logging]
    - nginx
    - xray
    - sandstorm
